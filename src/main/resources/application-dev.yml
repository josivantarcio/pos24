spring:
  datasource:
    url: jdbc:h2:mem:pos24db
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

logging:
  level:
    com.pos24: DEBUG
    org.springframework.security: DEBUG 